import { baseUrl, baseUrlWareHose } from "../store/store";

export const SITE_MAP = {
  getBoxesData: `${baseUrlWareHose}/get-boxes`,
  craeateBooking: `${baseUrl}/create-booking`,
  getBookingData: `${baseUrl}/get-booking`,
  getAllBooking: `${baseUrl}/get-all-booking`,
};
